tosca_definitions_version: dell_1_1

description: >
  This blueprint deploys SeaweedFS with Caddy reverse proxy on an existing edge-cloud-init deployment

imports:
  - dell/types/types.yaml
  - tech-ops/definitions.yaml
  - tech-ops/inputs.yaml
  - tech-ops/outputs.yaml

input_groups:

  seaweedfs_config:
    display_label: SeaweedFS Configuration
    collapsible: true
    index: 1
    inputs:
      - host_fqdn
      - swfs_image
      - caddy_image
      - swfs_user
      - swfs_password
      - swfs_admin_fqdn
      - swfs_admin_port
      - swfs_master_fqdn
      - swfs_master_port
      - enable_mq
      - mq_broker_port

  object_storage_config:
    display_label: Object StorageConfiguration
    collapsible: true
    index: 2
    inputs:
      - swfs_filer_fqdn
      - swfs_filer_port
      - default_filer_dir_name
      - artifacts_to_download
      - swfs_s3_fqdn
      - swfs_s3_port
      - s3_user
      - s3_bucket
      - enable_smb
      - enable_nfs

  monitoring_config:
    display_label: Monitoring Configuration
    collapsible: true
    index: 3
    inputs:
      - enable_monitoring
      - loki_image
      - grafana_image
      - prometheus_ext_image
      - grafana_fqdn
      - grafana_port
      - loki_port
      - prometheus_ext_port
      - loki_bucket
      - cluster_name

  env_config:
    display_label: Environment Configuration
    collapsible: true
    index: 4
    inputs:
      - run_arg
      - script_url
      - offline_mode
      - offline_binary_secret
      - registry_secret
      - registry_url
      - upload_package
      - upload_binary_secret
      - hide_log_output
      - debug

labels:
  csys-obj-type:
    values:
      - environment
  target_environment:
    values:
      - ned
  vendor:
    values:
      - tme-tech-ops
  solution:
    values:
      - seaweedfs-tech-ops
  version:
    values:
      - "1.0.0.0"

blueprint_labels:
  env:
    values:
      - NED
